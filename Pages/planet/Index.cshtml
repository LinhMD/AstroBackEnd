@page
@model AstroBackEnd.Pages.planet.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Planet[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Planet[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Planet[0].Icon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Planet[0].Decription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Planet[0].Tag)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Planet[0].MainContent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Planet) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Icon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Decription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tag)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MainContent)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
